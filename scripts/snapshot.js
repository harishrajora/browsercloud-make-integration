// SNAPSHOT_SCRIPT — runs in the cloud browser via WebDriver execute/sync.
// TODO: port from n8n SNAPSHOT_SCRIPT; rename data-n8n-ref -> data-ref.
// Must return: { elements: [{ref,tag,role,text}], text: "1. <tag> [role] text\n..." }
